// The message should appear in browser console (browser can be opened via Live Server plugin for VS code).
// console.log('This js file works!'); //Commented because its only needed to test if index.html can load this js file.
